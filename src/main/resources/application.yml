server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:h2:file:./data/blogdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always
